apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-egress
  namespace: day-5
spec:
  podSelector:
    matchLabels:
      project: policy-test
  policyTypes:
  - Egress
