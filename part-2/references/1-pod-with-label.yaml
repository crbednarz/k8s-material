apiVersion: v1
kind: Pod
metadata:
  labels:
    run: nginx
    app: nginx-lab
    owner: your_name
  name: nginx
spec:
  containers:
  - image: nginx
    name: nginx
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
